API_SECRET=change_me_to_a_long_random_string
PORT=3000
# ── Standalone / Atlas ─────────────────────────────────────────────────────────
# Uncomment and fill in when running outside of Docker Compose.
# MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/health_mcp?retryWrites=true&w=majority
